/**
 * Composables Index
 * 
 * Central export point for all Vue composables (hooks).
 * Following Clean Architecture: composables provide a clean interface
 * to application logic, keeping components thin and focused on presentation.
 */

export { useAuth } from './useAuth'
export { useLoading } from './useLoading'
