# 🏗️ Arquitetura do Sistema - Hockey Club Management

## 📋 Visão Geral

```
┌─────────────────────────────────────────────────────────────┐
│                     Hockey Club Management                   │
│                   Sistema de Gestão Completo                │
└─────────────────────────────────────────────────────────────┘
                              │
                              │
              ┌───────────────┴───────────────┐
              │                               │
              ▼                               ▼
    ┌──────────────────┐            ┌──────────────────┐
    │   Frontend (SPA) │            │   Backend (API)  │
    │                  │            │                  │
    │   Vue 3 + Vite   │◄──────────►│     NestJS       │
    │   TypeScript     │   REST API │   TypeScript     │
    │   Tailwind CSS   │            │   Swagger        │
    └──────────────────┘            └──────────────────┘
              │                               │
              │                               │
              ▼                               ▼
    ┌──────────────────┐            ┌──────────────────┐
    │  State Management│            │    Supabase      │
    │      Pinia       │            │  PostgreSQL +    │
    │   Persistência   │            │   Auth + Storage │
    └──────────────────┘            └──────────────────┘
```

---

## 🎯 Clean Architecture - Camadas

### Frontend (Vue 3)

```
┌─────────────────────────────────────────────────────────────┐
│                     PRESENTATION LAYER                       │
│  • Vue Components (.vue files)                              │
│  • Pages (LoginPage, DashboardPage)                         │
│  • Layouts (AdminLayout, AthleteLayout)                     │
│  • Router (navigation + guards)                             │
│                                                             │
│  Responsabilidade: Renderizar UI e capturar eventos        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    APPLICATION LAYER                         │
│  • Composables (useAuth, useLoading)                        │
│  • Stores (authStore, loadingStore)                         │
│  • Route Guards (authentication, authorization)             │
│                                                             │
│  Responsabilidade: Orquestrar lógica de aplicação          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       DOMAIN LAYER                           │
│  • Types (User, Event, Debt, etc.)                          │
│  • Business Rules (validation rules)                        │
│  • Entities (domain objects)                                │
│                                                             │
│  Responsabilidade: Definir regras de negócio               │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  INFRASTRUCTURE LAYER                        │
│  • API Clients (axios, fetch)                               │
│  • Supabase SDK                                             │
│  • LocalStorage                                             │
│  • External Services                                        │
│                                                             │
│  Responsabilidade: Integração com sistemas externos        │
└─────────────────────────────────────────────────────────────┘
```

### Backend (NestJS)

```
┌─────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                        │
│  • Controllers (endpoints REST)                             │
│  • DTOs (Data Transfer Objects)                             │
│  • Swagger Decorators                                       │
│                                                             │
│  Responsabilidade: Expor API HTTP                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    APPLICATION LAYER                         │
│  • Services (business logic)                                │
│  • Guards (authentication, authorization)                   │
│  • Interceptors (logging, transformation)                   │
│                                                             │
│  Responsabilidade: Processar requisições                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       DOMAIN LAYER                           │
│  • Entities (domain models)                                 │
│  • Types (shared types from package)                        │
│  • Business Rules                                           │
│                                                             │
│  Responsabilidade: Lógica de negócio pura                  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  INFRASTRUCTURE LAYER                        │
│  • Supabase Client                                          │
│  • Database Access                                          │
│  • Email Service                                            │
│  • File Storage                                             │
│                                                             │
│  Responsabilidade: Persistência e serviços externos        │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Fluxo de Dados - Autenticação

```
┌─────────────┐
│   Usuário   │
└──────┬──────┘
       │
       │ 1. Preenche email/senha
       ▼
┌─────────────────┐
│   LoginPage     │  (Presentation)
│   Component     │
└────────┬────────┘
         │
         │ 2. Chama composable
         ▼
┌─────────────────┐
│   useAuth()     │  (Application)
│   Composable    │
└────────┬────────┘
         │
         │ 3. Dispara action
         ▼
┌─────────────────┐
│   authStore     │  (Application)
│   Pinia Store   │
└────────┬────────┘
         │
         │ 4. Valida credenciais (mock em Fase 1)
         ▼
┌─────────────────┐
│  mockLogin()    │  (Infrastructure - será API em Fase 2)
│   Function      │
└────────┬────────┘
         │
         │ 5. Retorna User + Token
         ▼
┌─────────────────┐
│   authStore     │  (Application)
│  Atualiza State │
└────────┬────────┘
         │
         │ 6. Persiste no localStorage
         ▼
┌─────────────────┐
│  localStorage   │  (Infrastructure)
└────────┬────────┘
         │
         │ 7. Redireciona baseado em role
         ▼
┌─────────────────┐
│  Vue Router     │  (Presentation)
│  Navigation     │
└────────┬────────┘
         │
         │ 8. Renderiza dashboard
         ▼
┌─────────────────┐
│  DashboardPage  │  (Presentation)
│   Component     │
└─────────────────┘
```

---

## 🔒 Proteção de Rotas - RBAC

```
┌─────────────────────────────────────────────────────────────┐
│                        Vue Router                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ Usuário tenta acessar rota
                              ▼
┌─────────────────────────────────────────────────────────────┐
│               beforeEach Guard #1: Autenticação              │
│                                                              │
│   ┌──────────────────────────────────────────────────┐     │
│   │  const requiresAuth = route.meta.requiresAuth    │     │
│   │  const isAuthenticated = authStore.isAuth        │     │
│   │                                                   │     │
│   │  if (requiresAuth && !isAuthenticated) {         │     │
│   │    redirect → /login                              │     │
│   │  }                                                │     │
│   └──────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ Usuário está autenticado
                              ▼
┌─────────────────────────────────────────────────────────────┐
│               beforeEach Guard #2: Autorização               │
│                                                              │
│   ┌──────────────────────────────────────────────────┐     │
│   │  const allowedRoles = route.meta.allowedRoles    │     │
│   │  const userRole = authStore.userRole             │     │
│   │                                                   │     │
│   │  if (!allowedRoles.includes(userRole)) {         │     │
│   │    redirect → dashboard correto para role        │     │
│   │  }                                                │     │
│   └──────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ Usuário tem permissão
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Renderiza Componente                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Fluxo de Dados - Stores (Pinia)

```
┌─────────────────────────────────────────────────────────────┐
│                         Component                            │
│                                                              │
│   <script setup>                                            │
│     import { useAuth } from '@/composables'                 │
│     const { login, user, isAdmin } = useAuth()              │
│   </script>                                                 │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ Composable expõe interface limpa
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                      useAuth Composable                      │
│                                                              │
│   function useAuth() {                                      │
│     const authStore = useAuthStore()                        │
│     return {                                                │
│       user: authStore.user,                                 │
│       login: (email, pwd) => authStore.login(email, pwd),   │
│       isAdmin: authStore.isAdmin,                           │
│     }                                                       │
│   }                                                         │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ Composable delega para store
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                       authStore (Pinia)                      │
│                                                              │
│   State:                                                    │
│     • user: User | null                                     │
│     • accessToken: string | null                            │
│     • isLoading: boolean                                    │
│                                                              │
│   Getters (Computed):                                       │
│     • isAuthenticated: boolean                              │
│     • userRole: UserRole                                    │
│     • isAdmin: boolean                                      │
│                                                              │
│   Actions:                                                  │
│     • login(email, password)                                │
│     • logout()                                              │
│     • setUser(user)                                         │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ Store persiste automaticamente
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              localStorage (pinia-persist plugin)             │
│                                                              │
│   Key: auth                                                 │
│   Value: { user, accessToken }                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎨 Design System - Tokens

```
┌─────────────────────────────────────────────────────────────┐
│                   design-tokens.ts                           │
│                  (Single Source of Truth)                    │
│                                                              │
│  export const designTokens = {                              │
│    colors: {                                                │
│      primary: 'hsl(24, 100%, 50%)',  // Laranja            │
│      secondary: 'hsl(0, 0%, 10%)',   // Preto              │
│      background: 'hsl(0, 0%, 100%)', // Branco             │
│    },                                                       │
│    spacing: { sm: '8px', md: '16px', ... },                │
│    typography: { fontSize: {...}, fontWeight: {...} },     │
│    shadows: { sm: '...', md: '...', ... },                 │
│  }                                                          │
└────────────────┬────────────────────────────────────────────┘
                 │
                 │ Importado por
                 │
     ┌───────────┼───────────┐
     │           │           │
     ▼           ▼           ▼
┌─────────┐ ┌─────────┐ ┌─────────┐
│  CSS    │ │Components│ │Tailwind │
│Variables│ │(inline)  │ │Config   │
│         │ │          │ │         │
│ main.css│ │ :style=" │ │tailwind.│
│         │ │  {color: │ │config.js│
│ :root { │ │  tokens} │ │         │
│  --pri..│ │"         │ │ theme:{ │
│ }       │ │          │ │  extend │
│         │ │          │ │ }       │
└─────────┘ └─────────┘ └─────────┘
```

---

## 🔄 Fluxo Completo - Caso de Uso: Admin Marca Presença (Fase 2)

```
┌─────────────┐
│    Admin    │ 1. Acessa /admin/attendance
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│ AttendancePage.vue  │ 2. Renderiza lista de eventos
└──────┬──────────────┘
       │
       │ 3. useAttendance() composable
       ▼
┌─────────────────────┐
│ attendanceStore     │ 4. Busca eventos (getEvents action)
└──────┬──────────────┘
       │
       │ 5. Chama API
       ▼
┌─────────────────────┐
│ axios.get('/events')│ 6. HTTP Request
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ NestJS Controller   │ 7. EventsController.findAll()
└──────┬──────────────┘
       │
       │ 8. Guard verifica JWT
       ▼
┌─────────────────────┐
│ JwtAuthGuard        │ 9. Valida token + role
└──────┬──────────────┘
       │
       │ 10. Autorizado
       ▼
┌─────────────────────┐
│ EventsService       │ 11. Business logic
└──────┬──────────────┘
       │
       │ 12. Query database
       ▼
┌─────────────────────┐
│ Supabase Client     │ 13. SELECT * FROM events
└──────┬──────────────┘
       │
       │ 14. Retorna dados
       ▼
┌─────────────────────┐
│ Response DTO        │ 15. Formata resposta
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ attendanceStore     │ 16. Atualiza state
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ AttendancePage.vue  │ 17. Re-renderiza UI
└─────────────────────┘
       │
       │ 18. Admin marca presença
       ▼
┌─────────────────────┐
│ POST /attendance    │ 19. Envia dados
└─────────────────────┘
       │
       │ ... (ciclo se repete)
       ▼
```

---

## 📦 Estrutura de Pacotes (Monorepo)

```
hockey-club-management/
│
├── apps/
│   ├── web/                    Frontend (Vue 3)
│   │   ├── src/
│   │   │   ├── assets/        Estilos, imagens
│   │   │   ├── components/    Componentes reutilizáveis
│   │   │   ├── composables/   Lógica compartilhada
│   │   │   ├── layouts/       Layouts por role
│   │   │   ├── lib/           Utilities + design tokens
│   │   │   ├── pages/         Páginas da aplicação
│   │   │   ├── router/        Configuração de rotas
│   │   │   ├── stores/        Estado global (Pinia)
│   │   │   ├── App.vue        Componente raiz
│   │   │   └── main.ts        Entry point
│   │   └── package.json
│   │
│   └── api/                    Backend (NestJS)
│       ├── src/
│       │   ├── modules/       Feature modules (Fase 2)
│       │   │   ├── auth/
│       │   │   ├── users/
│       │   │   ├── events/
│       │   │   ├── attendance/
│       │   │   ├── debts/
│       │   │   └── financial/
│       │   ├── app.module.ts  Root module
│       │   └── main.ts        Entry point
│       └── package.json
│
└── packages/
    └── types/                  Tipos compartilhados
        ├── src/
        │   ├── user.types.ts
        │   ├── event.types.ts
        │   ├── attendance.types.ts
        │   ├── debt.types.ts
        │   ├── financial.types.ts
        │   └── index.ts
        └── package.json
```

---

## 🎯 Princípios de Design

### 1. Separation of Concerns
- **UI**: Apenas renderização e eventos
- **Logic**: Em stores e composables
- **Data**: Tipos e entidades separados

### 2. Dependency Inversion
- Componentes dependem de **abstrações** (composables)
- Não dependem de **implementações** (stores direto)

### 3. Single Responsibility
- Cada arquivo/módulo tem **um propósito**
- Stores por **feature**, não por tipo de dado

### 4. Open/Closed Principle
- Extensível para novas features
- Sem modificar código existente

### 5. Interface Segregation
- Composables expõem **apenas o necessário**
- Não vazam detalhes de implementação

---

## 📈 Escalabilidade

### Adicionar Nova Feature

```
1. Criar tipos em packages/types/src/
2. Criar store em apps/web/src/stores/
3. Criar composable em apps/web/src/composables/
4. Criar páginas em apps/web/src/pages/
5. Adicionar rotas em apps/web/src/router/
6. Backend: criar module em apps/api/src/modules/
7. Backend: controller + service + DTOs
```

### Performance

- **Code Splitting**: Páginas carregadas sob demanda (`() => import()`)
- **Lazy Components**: Componentes pesados carregados quando necessário
- **Pinia Persist**: Apenas dados críticos (user, token)
- **Vite HMR**: Hot reload instantâneo no desenvolvimento

---

**Arquitetura projetada para crescer sem refatorações massivas** 🚀
